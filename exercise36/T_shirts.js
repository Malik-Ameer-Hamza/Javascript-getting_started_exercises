function make_shirt(size, text) {
    console.log("The size of the shirt is ".concat(size, " and message to print on it is \"").concat(text, "\""));
}
make_shirt(40, "I don't need Google, my wife knows everything.");
