function make_shirt(size: number, text: string) {
    console.log(`The size of the shirt is ${size} inches, and message to print on it is "${text}"`);

}
make_shirt(40, "I don't need Google, my wife knows everything.")